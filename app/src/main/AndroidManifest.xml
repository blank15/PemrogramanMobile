<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yudistirosaputro.gmail.com.pertemuan4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".pertemuanKe5.Pertemuan5"></activity>
        <activity android:name=".pertemuan6.Pertemuan6">
        </activity>

        <service
            android:name=".pertemuan6.PlayAudio"
            android:enabled="true" />

        <activity android:name=".pertemuan8.TugasPertemuan8">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>